import {StyleSheet} from 'react-native';
import * as appStyles from 'app-global-styles.json';


export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
    minHeight: 2 * appStyles.itemDimension,
    marginHorizontal: '5%',
    width: '90%',
    borderRadius: appStyles.itemBorderRadius,
  },
})


// id unit
// photo from gallery
