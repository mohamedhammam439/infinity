import {lazy} from 'react';
export const boolean = lazy(()=>import('./boolean'))
export const text = lazy(()=>import('./text'))
export const password = lazy(()=>import('./password'))
export const label = lazy(()=>import('./label'))
